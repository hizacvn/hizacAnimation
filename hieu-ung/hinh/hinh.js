// Lấy phần tử triangle
var triangle = document.getElementById('triangle');

// Tạo một phần tử con
var innerTriangle = document.createElement('div');
innerTriangle.className = 'inner-triangle';

// Thêm phần tử con vào phần tử triangle
triangle.appendChild(innerTriangle);
